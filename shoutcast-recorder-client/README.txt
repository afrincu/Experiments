client_radio.php creates a client that connects to an online radio, using the shoutcast protocol. 

It records the content from the radio-s server, excluding the metadata information. 
After each metadata interval it creates an audio file (.mp3, .aac) with the name found in the metadata.
